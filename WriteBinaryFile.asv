clear all;
close all;

%
%   Zapis barkrovej postupnosti do binarneho suboru.
%

gen = comm.BarkerCode( ...
    'SamplesPerFrame',64, ...
    'Length',13, ...
    'OutputDataType', ...
    'int8');
seq = gen.step();

f = fopen('OFDMHead.bin','w');
fwrite(f,seq,'uint8');
fclose(f);

seq2 = ones(1,88);
f2 = fopen('OFDMData.bin','w');
fwrite(f,seq,'uint8');
fclose(f);


